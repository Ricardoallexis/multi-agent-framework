# Multi-Agent Framework — Human Research Normalization v1

Convert the external response provided by the human into the `ResearchOutput` contract without adding new information. Preserve URLs, uncertainties, contradictions, dates, and numbers. If a field is missing, use an empty list or mark grounding as `PARTIAL`/`UNVERIFIED` as appropriate. Do not invent sources or facts.
